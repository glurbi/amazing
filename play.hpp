#ifndef _play_hpp_
#define _play_hpp_

#include "context.hpp"
#include "model.hpp"

void play(maze_model& model, sf::RenderWindow& window, color color, sf::Font& font, sf::Text& text);

#endif
